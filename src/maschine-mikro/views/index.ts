import { BaseView } from './BaseView';
import { PatternView } from './PatternView';
import { SceneView } from './SceneView';

export const views = {
    BASE: BaseView,
    PATTERN: PatternView,
    SCENE: SceneView,
};
